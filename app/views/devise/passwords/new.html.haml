.center-align.flow-text= devise_error_messages!
.password.container
  .flex
    %h3.center-align Forgot your password?
    %br/
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      .field
        = f.label :email
        = f.email_field :email, autofocus: true, class: 'validate'
      %br/
      .actions.center-align
        %button.waves-effect.waves-light.btn#devise_button{ type: 'submit', name: 'commit', 'data-disable-with': 'Reset'} Reset
    %br/
    .center-align= render 'devise/shared/links'